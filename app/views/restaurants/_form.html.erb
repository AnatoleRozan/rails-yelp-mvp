 <%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :category, collection: Restaurant::CATEGORIES %>
  <%= f.input :phone_number %>
  <%= f.submit class: "btn btn-primary mt-4" %>
  <%#= link_to "Delete", restaurant_path(Restaurant.last), data: { turbo_method: :delete, turbo_confirm: "Are your sure?" } %>
  <%#= button_to "Delete", restaurant_path(Restaurant.last), method: :delete, form: { data: { turbo_confirm: "Are your sure?" } } %>
<% end %>
